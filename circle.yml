machine:
  node:
    version: 5.2.0

dependencies:
  override:
    - npm i -g npm@3.x
    - npm i
  cache_directories:
    - "../node_modules"

deployment:
  main:
    branch: master
    heroku:
      appname: buildboard
